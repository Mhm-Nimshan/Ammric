import React from 'react'
import PathView from "../HOC/PathView"
const index = () => {
    return (
        <div>
            this is Parameters
        </div>
    )
}

export default PathView(index)
