const path = {
    PROGRAMS: "/programs",
    LOCATIONS: "/locations",
    SECURITY: "/security",
    PARAMETERS: "/parameter",
    SETTINGS: "/settings",
    HOME: "/"
}

const subLinks = {
    locations: ["council", "communities"]
}

export { subLinks };

export default path;